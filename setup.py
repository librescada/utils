from setuptools import setup, find_packages

setup(
    name='librescada_utils',
    version='0.2',  # Update with your desired version
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)